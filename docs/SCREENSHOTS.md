# Screenshots

Before publishing the project, capture the main window in its stopped and active states. Save the images in this directory with descriptive names, for example:

- `main-window-stopped.png`
- `main-window-active.png`

Then add them to the root README under a `Screenshots` heading. This keeps documentation images separate from the application source.
